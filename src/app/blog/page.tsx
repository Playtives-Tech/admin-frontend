'use client';
import Link from 'next/link'; import { useEffect, useState } from 'react'; import { DashboardShell } from '@/components/dashboard/shell'; import { blogAdminService, type BlogPost } from '@/lib/services/blog-service';
export default function BlogPage(): React.JSX.Element { const [items, setItems] = useState<BlogPost[]>([]); useEffect(() => { void blogAdminService.list().then((data) => setItems(data.items)); }, []); return <DashboardShell title="Blog" description="Create, preview, publish, and manage articles"><div className="mx-auto max-w-6xl"><div className="mb-7 flex items-center justify-between"><div><h2 className="text-3xl font-semibold">Articles</h2><p className="mt-1 text-sm text-muted-foreground">Draft thoughtfully, then publish when ready.</p></div><Link href="/blog/new" className="rounded-xl bg-brand px-4 py-2.5 text-sm font-semibold text-brand-foreground">New article</Link></div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{items.map((post) => <Link key={post._id} href={`/blog/${post._id}`} className="app-surface rounded-2xl border p-5"><div className="flex justify-between"><span className="text-xs font-bold text-brand">{post.status}</span><span className="text-xs text-muted-foreground">{post.viewCount} views</span></div><h3 className="mt-4 line-clamp-2 text-lg font-semibold">{post.title}</h3><p className="mt-2 line-clamp-2 text-sm text-muted-foreground">{post.excerpt}</p><p className="mt-5 text-xs text-muted-foreground">{post.likeCount} likes · {post.commentCount} comments</p></Link>)}</div>{items.length === 0 ? <p className="rounded-2xl border border-dashed p-12 text-center text-muted-foreground">No articles yet.</p> : null}</div></DashboardShell>; }
